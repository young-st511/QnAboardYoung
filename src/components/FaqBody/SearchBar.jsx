import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <form>
        <input placeholder='검색' />
        <button name='검색' type='button' />
      </form>
    </div>
  );
};

export default SearchBar;
